
/**
 * INSTRUCTIONS POUR L'INSTALLATION SUPABASE :
 * 
 * 1. Installez le client : npm install @supabase/supabase-js
 * 2. Créez un projet sur supabase.com
 * 3. Copiez les clés API ci-dessous.
 */

/*
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'VOTRE_SUPABASE_URL';
const supabaseAnonKey = 'VOTRE_SUPABASE_ANON_KEY';

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
*/

// NOTE: Dans cet exemple autonome, nous utilisons des données mockées.
// Pour passer en production, décommentez le code ci-dessus et implémentez
// les appels fetch dans App.tsx.
